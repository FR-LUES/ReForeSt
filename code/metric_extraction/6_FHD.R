# This script takes normalised point clouds and calculates foliage height diversity. 
# This is calculated across 2 m bins and relative densities are used rather than raw point clouds.


# Now run in execution script ---- !#
# Preamble
# source("code/0_setup.R")
# source("code/1_Functions.R")
# # Load in data ---- !#
# clipped <- readLAScatalog(path_test_lasClipped)# Read in LiDAR data
# # read in shapefiles
# shapes <- st_read(paste0(path_test_data_shp, "testShapes_buffered.gpkg"))
# ---- !#

